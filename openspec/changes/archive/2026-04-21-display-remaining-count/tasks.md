## 1. Tests

- [x] 1.1 Write failing tests for remaining count display in `test/remaining-count.test.js`

## 2. Implementation

- [x] 2.1 Add footer HTML element with `id="todo-count"` between `#todo-list` and `.filter-bar` in `src/index.html`
- [x] 2.2 Add CSS styles for the footer in `src/index.html`
- [x] 2.3 Update `render()` in `src/index.html` to compute active count and set footer text with singular/plural logic
